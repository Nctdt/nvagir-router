{
  "name": "nvagir-router",
  "version": "1.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "prepublish": "tsc --build --clean && tsc --build"
  },
  "keywords": [
    "simple router"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Nctdtman/nvagir-router.git"
  },
  "author": "nctdtman",
  "license": "MIT",
  "dependencies": {
    "nvagir": "^1.2.0"
  }
}
